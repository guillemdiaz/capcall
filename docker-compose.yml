services:
  web:
    build: ./backend
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./backend:/code
    depends_on:
      - db

  db:
    image: postgres:16
    container_name: capcall-db
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=trust"

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - web

  redis:
    image: redis:8-alpine
    container_name: capcall-redis
    ports:
      - "6379:6379"

  celery:
    build: ./backend
    container_name: capcall-celery
    command: watchfiles --filter python "celery -A config worker -l INFO"
    volumes:
      - ./backend:/code
    depends_on:
      - redis
      - db

  flower:
    build: ./backend
    container_name: capcall-flower
    command: celery -A config flower --port=5555
    ports:
      - "5555:5555"
    volumes:
      - ./backend:/code
    depends_on:
      - redis
      - celery

volumes:
  postgres_data: